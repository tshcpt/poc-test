apiVersion: v1
kind: Service
metadata:
  name: poc-service
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: nlb
spec:
  type: NodePort
  selector:
    app: poc
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30301
  type: LoadBalancer